# Generate with: openssl rand -hex 32
KEN_TOKEN=

# Anthropic API key — required for chat
ANTHROPIC_API_KEY=

# OpenAI API key — required for voice transcription (Whisper). Optional otherwise.
OPENAI_API_KEY=

# Telegram bot — get token from @BotFather, chat_id from /getUpdates
TELEGRAM_BOT_TOKEN=
ALLOWED_CHAT_IDS=
